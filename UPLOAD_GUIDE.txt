HYDROGEOAI LAB v4.1 — WEB UPLOAD FRIENDLY

Every module now contains:
- TEMPLATE_*.txt  -> blank standardized form
- README.txt      -> short instructions

IMPORTANT:
GitHub does not need empty folders anymore because every folder has a template/readme.

WORKFLOW:
GitHub web may hide .github. Create it manually:
Add file -> Create new file
Name:
.github/workflows/build-data.yml

Then paste the complete contents of:
WORKFLOW_COPY_build-data.yml

After that, content updates should trigger automatically on Commit.
